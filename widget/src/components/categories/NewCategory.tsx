function NewCategory(props: { className?: string }) {
  return (
    <div className={`bg-blue-500 rounded-full p-1 text-gray-100 flex justify-center items-center ${ props.className ? props.className : "w-12" } {
      constructor(parameters) {
        
      }
    }}`}>
      New
    </div>
  );
}

export default NewCategory;
